apiVersion: v1
kind: Service
metadata:
  name: altruist-prometheus
  namespace: altruist
spec:
  ports:
   - name: "9090"
     port: 9090
     targetPort: 9090
  type: ClusterIP
  selector:
    app: altruist-prometheus

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: altruist-prometheus
  namespace: altruist
spec:
  replicas: 1
  selector:
    matchLabels:
      app: altruist-prometheus
  template:
    metadata:
      labels:
        app: altruist-prometheus
    spec:   
      containers:
      - image: prom/prometheus #missing version
        name: altruist-prometheus
        ports:
          - containerPort: 9090
            protocol: TCP
        volumeMounts:
          - mountPath: /etc/prometheus/prometheus.yml
            name: prometheus-cm0
            readOnly: true
            subPath: prometheus.yml
      restartPolicy: Always
      volumes:
      - configMap:
          items:
            - key: prometheus.yml
              path: prometheus.yml
          name: prometheus-cm0
        name: prometheus-cm0
