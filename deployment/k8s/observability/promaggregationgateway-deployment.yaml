apiVersion: v1
kind: Service
metadata:
  name: __TEMPLATE_PROJECT_NAME__-prom-aggregation-gateway
  namespace: __TEMPLATE_PROJECT_NAME__
spec:
  ports:
    - name: "9091"
      port: 9091
      targetPort: 80
  type: ClusterIP
  selector:
    app: __TEMPLATE_PROJECT_NAME__-prom-aggregation-gateway
  



---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: __TEMPLATE_PROJECT_NAME__-prom-aggregation-gateway
  namespace: __TEMPLATE_PROJECT_NAME__
spec:
  replicas: 1
  selector:
    matchLabels:
      app: __TEMPLATE_PROJECT_NAME__-prom-aggregation-gateway
  template:
    metadata:
      labels:
        app: __TEMPLATE_PROJECT_NAME__-prom-aggregation-gateway
    spec:   
      containers:
      - name: __TEMPLATE_PROJECT_NAME__-prom-aggregation-gateway
        image: ghcr.io/zapier/prom-aggregation-gateway:v0.9.0        
        args:
        - --apiListen=:80        
        ports:
          - containerPort: 80
            protocol: TCP
      restartPolicy: Always




