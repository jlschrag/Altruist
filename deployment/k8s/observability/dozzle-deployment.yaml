apiVersion: v1
kind: Service
metadata:
  name: altruist-dozzle
  namespace: altruist
spec:
  ports:
    - name: "8089"
      port: 8089
      targetPort: 8089
      protocol: TCP
  type: ClusterIP
  selector:
    app: altruist-dozzle

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: altruist-dozzle
  namespace: altruist
spec:
  replicas: 1
  selector:
    matchLabels:
      app: altruist-dozzle
  template:
    metadata:
      labels:
        app: altruist-dozzle
    spec:   
      serviceAccountName: altruist-pod-viewer
      containers:
      - name: altruist-dozzle
        image: ghcr.io/amir20/dozzle:v8.12.3         
        ports:
          - containerPort: 8089
            protocol: TCP
        env:
            - name: DOZZLE_MODE
              value: "k8s"
            #unccomment the following line to set a namespace
            #- name: DOZZLE_NAMESPACE
            #  value: "altruist"
            - name: DOZZLE_ADDR
              value: ":8089"
      restartPolicy: Always

---

apiVersion: v1
kind: ServiceAccount
metadata:
  name: altruist-pod-viewer
  namespace: altruist

---

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: altruist-pod-viewer-binding
  namespace: altruist
subjects:
  - kind: ServiceAccount
    name: altruist-pod-viewer
    namespace: altruist
roleRef:
  kind: ClusterRole
  name: altruist-pod-viewer-role
  apiGroup: rbac.authorization.k8s.io

---

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: altruist-pod-viewer-role
  namespace: altruist
rules:
  - apiGroups: [""]
    resources: ["pods", "pods/log", "nodes"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["metrics.k8s.io"]
    resources: ["pods"]
    verbs: ["get", "list"]